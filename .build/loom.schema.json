{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "LoomSettings",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "workspace": {
      "$ref": "#/definitions/WorkspaceSettings"
    },
    "artifacts": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/definitions/ArtifactSettings"
      }
    },
    "global": {
      "description": "Global loom settings to use if artifact specifc are not set",
      "oneOf": [
        {
          "$ref": "#/definitions/GlobalSettings"
        }
      ]
    },
    "nuget": {
      "description": "Settings to use NugetUploadModule locally. Use environment variables in production",
      "oneOf": [
        {
          "$ref": "#/definitions/NugetSettings"
        }
      ]
    },
    "githubAccessToken": {
      "type": "string",
      "description": "Should not be set in dev"
    },
    "$schema": {
      "type": "string",
      "description": "The URL to the JSON schema for this file."
    }
  },
  "definitions": {
    "WorkspaceSettings": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "solution"
      ],
      "properties": {
        "solution": {
          "type": "string",
          "description": "Path to the solution file.",
          "minLength": 1
        },
        "artifactsPath": {
          "type": "string"
        },
        "cleanDirectories": {
          "type": "array",
          "description": "Additional directories to clean during the Clean target.",
          "items": {
            "type": "string"
          }
        },
        "enableNugetUpload": {
          "type": "boolean",
          "description": "Whether to upload NuGet packages during a release. Set to true to force, false to disable, or leave null to auto-detect based on local environment."
        },
        "enableGithubRelease": {
          "type": "boolean",
          "description": "Whether to create a GitHub release during a release."
        },
        "enableVelopackRelease": {
          "type": "boolean",
          "description": "Whether to create velopack packages during a release."
        }
      }
    },
    "ArtifactSettings": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "project",
        "type"
      ],
      "properties": {
        "project": {
          "type": "string",
          "description": "Path to the project file.",
          "minLength": 1
        },
        "type": {
          "description": "Artifact type (nuget, dotnet-publish, velopack).",
          "oneOf": [
            {
              "$ref": "#/definitions/ArtifactType"
            }
          ]
        },
        "rid": {
          "description": "Runtime identifier.",
          "$ref": "#/definitions/RuntimeIdentifier"
        },
        "version": {
          "type": "string"
        },
        "velopackId": {
          "type": "string"
        },
        "tagPrefix": {
          "type": "string"
        }
      }
    },
    "ArtifactType": {
      "type": "string",
      "description": "",
      "x-enumNames": [
        "Nuget",
        "Executable",
        "Velopack"
      ],
      "enum": [
        "Nuget",
        "Executable",
        "Velopack"
      ]
    },
    "GlobalSettings": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "target": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/definitions/BuildTarget"
            }
          ]
        },
        "configuration": {
          "type": "string"
        },
        "rid": {
          "$ref": "#/definitions/RuntimeIdentifier"
        },
        "version": {
          "type": "string"
        }
      }
    },
    "BuildTarget": {
      "type": "string",
      "description": "",
      "x-enumNames": [
        "Clean",
        "Restore",
        "Build",
        "Test",
        "Publish",
        "Release"
      ],
      "enum": [
        "Clean",
        "Restore",
        "Build",
        "Test",
        "Publish",
        "Release"
      ]
    },
    "NugetSettings": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "apiKey": {
          "type": "string"
        }
      }
    },
    "RuntimeIdentifier": {
      "type": "string",
      "enum": [
        "win-x64",
        "win-arm64",
        "linux-x64",
        "linux-arm64",
        "osx-x64",
        "osx-arm64"
      ]
    }
  }
}